---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: libvirt
lint:
  /bin/true
platforms:
  - name: instance
    synced_folder: true
    box: centos/7
    networks:
      - identifier: private_network
        options:
          ip: 192.168.0.2
provisioner:
  name: ansible
verifier:
  name: ansible
