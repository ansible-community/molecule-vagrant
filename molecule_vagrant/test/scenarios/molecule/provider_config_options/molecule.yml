---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: instance
    provider_options:
      default_nic_type: "82545EM"
    box: ${TESTBOX:-centos/7}
provisioner:
  name: ansible
verifier:
  name: ansible
